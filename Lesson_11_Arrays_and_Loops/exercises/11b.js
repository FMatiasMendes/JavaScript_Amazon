testArray = [1, 20, 22, 24, 5];
testArray2 = ['John', 'Mary', 'Phill'];

function getLastValue(array){

    const lastIndex = array.length - 1;
    console.log(array[lastIndex]);
}

getLastValue(testArray);

getLastValue(testArray2);

